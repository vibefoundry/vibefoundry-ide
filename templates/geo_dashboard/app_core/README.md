# geo_dashboard

Track 2 PWA — **Distribution Geo Dashboard** boilerplate. Vite-built variant (the project's only Vite-based Track 2 app — see project AGENTS.md for why plain-script is the default).

## What this is

A generic interactive geo dashboard for any "accounts × products × multi-period sales" dataset: filterable table + map, county heatmap, and state/county/tract boundary overlays. Runs entirely in the user's browser via DuckDB-WASM querying a Parquet file.

The default sample data ships an olive-oil distributor in the US Northeast (NY, NJ, MA, PA, CT, RI). Swap `sample_data/sample.parquet` (or drop a real parquet at `input_folder/app_data.parquet`) to use it for a different business.

## Genericization status

This app is at **Option A** of the A→B→C generic plan:

- **A (current)**: Top-of-file constants externalized to `public/data/app_config.json`. Inline column references in `js/app.js` still hardcoded. Same shape, same behavior — only the config schema is portable.
- **B (future)**: Replace inline domain labels (BATFS, Brand Variant, Visitation Priority) with config-driven display names so a different business can reuse the app via config swap.
- **C (future)**: Schema-agnostic engine — every inline column reference becomes a role lookup from config.

The forward-compatible `app_config.json` schema already includes fields (`role`, `label`, `filterable`, `color_map`) that are inert under A and ready for B/C to consume.

## Folder layout

The task root only holds operational entrypoints (the launcher trio + `build_app_package.py`).
Everything else lives under `app_core/` so the root stays clean.

```
geo_dashboard/
├── build_app_package.py        <- Reuses dev-asset prep, then assembles the Track 2 distributable
├── run_app.sh / run_app.bat    <- Local dev launcher (calls app_core/prepare_dev_assets.py + vite)
└── app_core/
    ├── prepare_dev_assets.py   <- Stages data + browser libs for local Vite dev and packaging
    ├── package.json, vite.config.js
    ├── src_app/
    │   ├── index.html, css/, js/  <- Source
    │   └── public/
    │       ├── data/
    │       │   ├── app_config.json  <- COMMITTED — source of truth for column metadata
    │       │   ├── manifest.json    <- COMMITTED — dataset list
    │       │   ├── *.parquet        <- gitignored, staged by the dev-prep flow
    │       │   └── boundaries/      <- gitignored, TIGER reference GeoJSONs
    │       └── lib/                 <- gitignored, DuckDB/React/Leaflet UMDs
    ├── sample_data/
    │   └── sample.parquet      <- COMMITTED — synthetic placeholder so app runs OOTB
    └── dist/, node_modules/    <- gitignored build artifacts (vite outputs to app_core/dist/)
```

## Data sourcing (build script priority)

`prepare_dev_assets.py` and `build_app_package.py` resolve the dataset in this order:
1. `input_folder/{file_from_app_config.json}` — real client data
2. `sample_data/sample.parquet` — synthetic fallback (small, committed)
3. Existing `src_app/public/data/{file}` if present (no-op)

The prep/build scripts print a banner indicating which source they used.

## Two dev workflows

| Goal | Command | What it does |
|---|---|---|
| Local testing with HMR | `bash run_app.sh` | Runs `prepare_dev_assets.py`, then starts `vite dev` on localhost:5173 |
| Explicit package build | `python3 build_app_package.py` | Reuses dev-asset prep, builds `dist/`, and assembles the distributable in `output_folder/` |

## Deleted in this refactor

- The Brand × ABCD priority matrix pane and its drilldown modal (per user request — not generally useful for re-use)
- All matrix-related state, queries, helpers, and config
- The broken `npm run prepare:assets` script reference in `package.json`
- Build artifacts (`dist/`, `node_modules/`) — gitignored, regenerated by build script
