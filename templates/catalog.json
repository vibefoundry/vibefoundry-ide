{
  "schema_version": 1,
  "templates": [
    {
      "id": "doc_scanner_backend",
      "name": "Doc Scanner — Backend (LLM Agent)",
      "track": 4,
      "icon": "document_scanner.png",
      "description": "A Python LLM agent that processes one record at a time. Watches an input folder, sends each item to a vision/text model with a structured prompt, validates the JSON response against a schema, retries on failure, and writes both a row-level CSV ledger and a per-record archive folder. The shipped baseline is a receipt scanner; the same scaffolding works for any per-record extraction or classification task.",
      "use_cases": [
        "Receipt / invoice / form extraction from photos or PDFs",
        "ID, license, or document field verification",
        "Per-image classification or tagging (products, defects, content moderation)",
        "Transcription cleanup or summarization of audio/text snippets, one file at a time",
        "Code-file review or annotation (one file = one LLM call)",
        "Any 'drop a file in, get a structured record out' workflow"
      ],
      "architecture": {
        "runtime": "Python 3.8+",
        "entry_point": "app.py",
        "launcher": "run_app.sh / run_app.bat (cd into the folder, python app.py)",
        "key_files": [
          { "path": "app.py", "role": "Orchestrator. Watches document_input/, dispatches files to a worker thread pool, coordinates logging." },
          { "path": "prompt.py", "role": "Builds the system + user prompt from instructions.json and output_schema.json, base64-encodes images, calls the LLM." },
          { "path": "retry.py", "role": "Wraps the LLM call. Re-asks on missing fields or network errors; exponential backoff on 429s." },
          { "path": "parse.py", "role": "Appends the validated record to classifications.csv. Thread-safe under concurrent writes." },
          { "path": "export.py", "role": "Copies the original input and the JSON record into agent_results/scanned/{record_id}/ for audit." },
          { "path": "instructions.json", "role": "Prompt configuration: system text, per-field rules, model choice, max_retries." },
          { "path": "output_schema.json", "role": "JSON Schema (strict) that the LLM output must match. Drives both validation and prompt generation." }
        ],
        "data_flow": "document_input/{file} → prompt.py builds request → LLM call (with retry.py) → parse.py appends row to agent_results/classifications.csv → export.py archives a copy under agent_results/scanned/{record_id}/",
        "dependencies": {
          "python": ["openai", "watchdog", "pillow"]
        },
        "customization": {
          "edit": [
            "instructions.json — change the system prompt, per-field rules, model, max_retries",
            "output_schema.json — change the fields the LLM must return (and any enums that drive the prompt)"
          ],
          "do_not_edit": ["app.py", "prompt.py", "retry.py", "parse.py", "export.py"],
          "env": {
            "OPENAI_API_KEY": "Required. Lives in .env next to run_app.sh.",
            "IMAGE_SCANNER_WORKERS": "Optional. Worker thread count (default tuned for typical desktops)."
          }
        }
      }
    },
    {
      "id": "dashboard_pwa_duckdb",
      "name": "Dashboard PWA — DuckDB-WASM",
      "track": 2,
      "icon": "data_analyst.png",
      "description": "A Progressive Web App that runs entirely in the browser. The user's data ships as Parquet files; DuckDB-WASM executes SQL client-side; React (loaded as a UMD bundle, no npm) renders a dataset picker, filterable table, and pagination. The output is a self-contained folder with a launcher per OS — zip it, hand it to anyone, and it works with no Python, no Node, and no install.",
      "use_cases": [
        "Interactive sales / KPI dashboards over a snapshot of company data",
        "Survey-result or research-data browsers shared with non-technical collaborators",
        "Read-only data explorers for journalism, civic data, or public reports",
        "Log or event-stream slicers built from a periodic export",
        "Catalog browsers (products, papers, datasets) with faceted filtering",
        "Any 'I want to share an interactive view of this dataset' app where the data fits in the browser"
      ],
      "architecture": {
        "runtime": "Browser (modern, WASM-capable). Python 3 only used at build time, not by end users.",
        "entry_point": "run_app.sh (dev) → prepare_dev_assets.py + serve.py. build_app_package.py produces the distributable.",
        "launcher": "run_app.sh / run_app.bat for dev; output_folder/{app}/mac_start.command, mac_start.sh, pc_start.bat for end users.",
        "key_files": [
          { "path": "build_app_package.py", "role": "Build script. Stages parquets from input_folder/, copies src_app/ to output_folder/, writes the end-user launcher trio." },
          { "path": "app_core/prepare_dev_assets.py", "role": "Dev helper. Syncs the parquets listed in app_config.json from input_folder/ into src_app/data/. Idempotent." },
          { "path": "app_core/serve.py", "role": "Local HTTP server for development. Picks a free port, registers .wasm and .parquet MIME types, opens the browser." },
          { "path": "app_core/src_app/index.html", "role": "Static shell. Loads React UMD, the DuckDB bundle, and js/app.js." },
          { "path": "app_core/src_app/js/app.js", "role": "React app (no JSX). Boots DuckDB-WASM, loads parquets, renders the dataset picker, filters, and table." },
          { "path": "app_core/src_app/data/app_config.json", "role": "App configuration: title and dataset list. Each dataset declares its file and the filter type per column (select / text / range / boolean)." },
          { "path": "app_core/src_app/lib/", "role": "Pre-bundled React 18, ReactDOM 18, and DuckDB-WASM (bundle + .wasm + worker). Committed so users don't need npm." },
          { "path": "CUSTOMIZE.md", "role": "Recipe. Tells the student which files to edit when forking and which to leave alone. Delete it after cloning." }
        ],
        "data_flow": "input_folder/*.parquet → prepare_dev_assets stages into src_app/data/ → browser fetches at runtime → DuckDB-WASM registers as a virtual file → SQL queries with filters + LIMIT/OFFSET → React table render",
        "dependencies": {
          "python_build_time_only": ["polars"],
          "browser_runtime": ["React 18 (UMD)", "ReactDOM 18 (UMD)", "DuckDB-WASM"]
        },
        "customization": {
          "edit": [
            "app_core/src_app/data/app_config.json — dataset list and per-column filter types",
            "app_core/src_app/index.html — page title"
          ],
          "do_not_edit": [
            "app_core/src_app/js/app.js",
            "app_core/src_app/styles.css",
            "app_core/src_app/lib/",
            "build_app_package.py",
            "app_core/prepare_dev_assets.py",
            "app_core/serve.py"
          ],
          "delete_after_cloning": ["CUSTOMIZE.md", "sample_data/", "any sample parquets shipped in src_app/data/"]
        }
      }
    },
    {
      "id": "doc_scanner_fullstack",
      "name": "Doc Scanner — Fullstack (React + Flask)",
      "track": 3,
      "icon": "document_scanner.png",
      "description": "A two-process app: a React frontend (Vite) talks to a Flask backend over /api/*. The shipped baseline wraps the Track 4 receipt-scanner agent in HTTP — the backend exposes upload + list + detail endpoints, streams scan progress over Server-Sent Events, and the frontend renders tables and an image preview. Use this when an app needs a live server at runtime (secrets, external APIs, mutations, auth, long-running jobs).",
      "use_cases": [
        "Web UI on top of an LLM agent so non-technical users can drop files in and see results stream back",
        "Internal admin tools that hit external APIs (CRMs, payment processors, vendor portals)",
        "RAG / semantic-search front ends backed by a local vector store and embeddings calls",
        "Workflow apps that mutate state (mark records reviewed, approve, re-run) and need a server to hold it",
        "Anything requiring auth, API keys, or secrets that can't live in the browser",
        "Long-running batch jobs where the UI needs live progress updates"
      ],
      "architecture": {
        "runtime": "Python 3.8+ (backend) and Node.js 16+ (frontend dev server). Modern browser.",
        "entry_point": "setup.sh (one-time install) then run_app.sh (launch both servers).",
        "launcher": "setup.sh / setup.bat for first-run install; run_app.sh / run_app.bat to launch backend + frontend concurrently on auto-picked ports; clear_cache.sh / clear_cache.bat for a clean rebuild.",
        "key_files": [
          { "path": "setup.sh", "role": "Idempotent first-run install. pip installs backend deps, npm installs frontend deps." },
          { "path": "run_app.sh", "role": "Reserves two free ports (BACKEND_PORT, FRONTEND_PORT), launches Flask + Vite concurrently." },
          { "path": "clear_cache.sh", "role": "Removes node_modules, __pycache__, and build artifacts, then re-runs setup." },
          { "path": "backend/app.py", "role": "Flask app. Endpoints: POST /api/scan (multipart upload → SSE stream), GET /api/receipts, GET /api/line_items, GET /api/receipts/<id>/items, GET /api/image/<id>." },
          { "path": "backend/prompt.py / parse.py / retry.py / export.py", "role": "Verbatim role-named modules from the doc_scanner_backend template. Same retry, schema validation, and archive logic, now driven by HTTP requests instead of a folder watcher." },
          { "path": "backend/instructions.json", "role": "Prompt configuration for the wrapped agent." },
          { "path": "backend/output_schema.json", "role": "JSON Schema the LLM output must match." },
          { "path": "backend/requirements.txt", "role": "Backend Python deps." },
          { "path": "frontend/src/App.jsx", "role": "React UI. Upload form, SSE stream consumer, receipt + line-item tables, detail drawer with image preview." },
          { "path": "frontend/vite.config.js", "role": "Reads FRONTEND_PORT and BACKEND_PORT env vars; proxies /api to the backend." },
          { "path": "frontend/package.json", "role": "Frontend deps: react, react-dom, vite, @vitejs/plugin-react." }
        ],
        "data_flow": "Browser upload → POST /api/scan (multipart) → Flask saves to backend/document_input/ → worker thread pool runs the per-file agent (prompt → LLM → retry → parse) → results appended to backend/agent_results/classifications.csv and archived under backend/agent_results/scanned/{id}/ → SSE event per completed record → frontend tables update live.",
        "dependencies": {
          "backend_python": ["flask", "flask-cors", "openai", "polars", "pillow"],
          "frontend_node": ["react", "react-dom", "vite", "@vitejs/plugin-react"],
          "dev_tooling": ["concurrently"]
        },
        "customization": {
          "edit_for_agent_behavior": [
            "backend/instructions.json",
            "backend/output_schema.json"
          ],
          "edit_for_ui": [
            "frontend/src/App.jsx and any sibling React components",
            "frontend/src styles"
          ],
          "do_not_edit": [
            "run_app.sh / setup.sh port-reservation logic",
            "vite.config.js proxy and CORS wiring",
            "the role-named .py filenames in backend/ (preserve prompt.py / parse.py / retry.py / export.py)"
          ],
          "env": {
            "OPENAI_API_KEY": "Required. Lives in .env next to run_app.sh.",
            "BACKEND_PORT": "Set automatically by run_app.sh; do not hardcode.",
            "FRONTEND_PORT": "Set automatically by run_app.sh; do not hardcode."
          },
          "notes": [
            "backend/document_input/ and backend/agent_results/ live inside the app folder, not at project-level input_folder/ or output_folder/. This keeps the app self-contained when zipped and shared."
          ]
        }
      }
    },
    {
      "id": "data_chatbot",
      "name": "Data Chatbot — Polars Code-Gen",
      "track": 3,
      "icon": "data_analyst.png",
      "description": "A conversational analytics app over parquet datasets. Flask backend exposes /api/ask as a Server-Sent Events stream; the React frontend renders the live stage progression (classify → codegen → execute → answer). The LLM never sees the rows — only a metadata profile (column names, dtypes, distinct values) generated from the parquets in data/. The codegen stage writes Polars code which the backend runs in an isolated subprocess; on traceback the error is fed back to the model and codegen retries up to max_retries. Each answered question lands in a timestamped folder with the code, result.parquet, and the verbal reply.",
      "use_cases": [
        "Natural-language analytics over a parquet warehouse — 'top 10 brands in Texas', 'avg volume by channel'",
        "Internal data Q&A bot for non-SQL stakeholders",
        "Ad-hoc reporting where the question keeps changing and a fixed dashboard is wrong",
        "Schema-aware data discovery — the bot stays within the columns and values you actually have",
        "Code-gen audit trail: every answered question ships its Polars code so the answer is verifiable"
      ],
      "architecture": {
        "runtime": "Python 3.8+ (backend) and Node.js 16+ (frontend dev server). Modern browser.",
        "entry_point": "setup.sh (one-time install) then run_app.sh (launch both servers).",
        "launcher": "setup.sh / setup.bat for first-run install; run_app.sh / run_app.bat to launch backend + frontend concurrently on auto-picked ports; clear_cache.sh / clear_cache.bat for a clean rebuild.",
        "key_files": [
          { "path": "backend/app.py", "role": "Flask orchestrator. Composes the per-question pipeline (classify → codegen+retry → execute → answer) and streams SSE events to the UI." },
          { "path": "backend/prompt.py", "role": "Builds the system + user prompt for each stage and calls the OpenAI API. Reads instructions.json + output_schema.json." },
          { "path": "backend/parse.py", "role": "Splits the codegen response into {code, response, error}." },
          { "path": "backend/execute.py", "role": "Runs the generated Polars code in a timeout-bounded subprocess and writes result.parquet." },
          { "path": "backend/retry.py", "role": "Codegen ↔ execute feedback loop; on traceback feeds the error back and regenerates up to max_retries." },
          { "path": "backend/history.py", "role": "Per-conversation memory. Always attaches the last 3 turns; classify picks additional relevant prior turns to include." },
          { "path": "backend/metadata.py", "role": "Profiles every parquet in data/ into the JSON column/value snapshot sent to the model." },
          { "path": "backend/instructions.json", "role": "Per-stage prompt config — system text, rules, model id, max_retries." },
          { "path": "backend/output_schema.json", "role": "JSON Schema the codegen reply must match." },
          { "path": "frontend/src/App.jsx", "role": "React chat UI. Consumes the SSE stream and renders stage progression, generated code, and the answer with a result preview." }
        ],
        "data_flow": "Browser POST /api/ask → backend opens SSE → classify (metadata-only vs needs code) → codegen (Polars code) → execute (subprocess writes result.parquet) → retry on traceback → answer (LLM phrases the result) → per-question folder in output/ holds code + result + answer.md.",
        "dependencies": {
          "backend_python": ["flask", "flask-cors", "openai", "polars", "pyarrow"],
          "frontend_node": ["react", "react-dom", "vite", "@vitejs/plugin-react"]
        },
        "customization": {
          "edit_for_chatbot_behavior": [
            "backend/instructions.json — system text, per-stage rules, model id, retry budget",
            "backend/output_schema.json — what the codegen stage must return"
          ],
          "edit_for_ui": [
            "frontend/src/App.jsx and sibling components"
          ],
          "drop_data": [
            "data/*.parquet — drop your own parquets here; metadata.py profiles them on startup"
          ],
          "do_not_edit": [
            "backend/execute.py subprocess isolation",
            "the role-named module filenames in backend/",
            "the SSE event protocol between backend and frontend"
          ],
          "env": {
            "OPENAI_API_KEY": "Required. Lives in .env next to run_app.sh."
          },
          "notes": [
            "The model never sees the data — only metadata.json. Row-level PII stays out of the prompt and the chatbot works over arbitrarily large parquets.",
            "data/ ships empty (gitignored). Drop your own parquets in and restart the backend to profile them."
          ]
        }
      }
    },
    {
      "id": "data_chatbot_codex",
      "name": "Data Chatbot — Codex CLI Variant",
      "track": 3,
      "icon": "data_analyst.png",
      "description": "Same conversational analytics app as data_chatbot — Flask backend, React frontend, SSE stage stream, Polars subprocess execution — except the codegen stage invokes the Codex CLI as a local subprocess instead of calling the OpenAI API. Use this when you want code generation routed through a local CLI agent (different rate limits, different auth posture, on-prem audit trail), or as a reference for swapping any LLM call for a CLI invocation while keeping the rest of the stack identical.",
      "use_cases": [
        "Same analytics use cases as data_chatbot, with codegen routed through Codex CLI instead of the API",
        "Environments where the LLM provider must be reached via a CLI agent rather than an HTTPS API",
        "Avoiding API per-token cost on the largest stage by using a local CLI workflow for codegen",
        "Reference for how to swap any single LLM call for a CLI invocation without touching the rest of the agent"
      ],
      "architecture": {
        "runtime": "Python 3.8+ (backend) and Node.js 16+ (frontend dev server). Modern browser. Codex CLI installed and on PATH for the codegen stage.",
        "entry_point": "setup.sh (one-time install) then run_app.sh (launch both servers).",
        "launcher": "setup.sh / setup.bat for first-run install; run_app.sh / run_app.bat to launch backend + frontend concurrently on auto-picked ports.",
        "key_files": [
          { "path": "backend/app.py", "role": "Flask orchestrator. Same pipeline as data_chatbot but routes the codegen stage through the Codex CLI." },
          { "path": "backend/prompt.py", "role": "Builds prompts for classify/codegen/answer. Codegen call is a subprocess invocation of the Codex CLI; classify and answer still use the OpenAI SDK." },
          { "path": "backend/execute.py", "role": "Runs the generated Polars code in a timeout-bounded subprocess and writes result.parquet." },
          { "path": "backend/retry.py", "role": "Feeds execution errors back into the next Codex CLI call for codegen retries." },
          { "path": "backend/instructions.json", "role": "Per-stage prompt config — system text, rules, model/CLI choice, retry budget." },
          { "path": "backend/output_schema.json", "role": "JSON Schema the codegen reply must match." },
          { "path": "frontend/src/App.jsx", "role": "Same chat UI as data_chatbot." }
        ],
        "data_flow": "Browser POST /api/ask → SSE stream → classify (OpenAI API) → codegen (Codex CLI subprocess) → execute (Polars subprocess) → retry on traceback → answer (OpenAI API).",
        "dependencies": {
          "backend_python": ["flask", "flask-cors", "polars", "pyarrow"],
          "frontend_node": ["react", "react-dom", "vite", "@vitejs/plugin-react"],
          "external_cli": ["Codex CLI (must be installed and on PATH)"]
        },
        "customization": {
          "edit_for_chatbot_behavior": [
            "backend/instructions.json — system text, per-stage rules, model/CLI args",
            "backend/output_schema.json — codegen contract"
          ],
          "edit_for_ui": [
            "frontend/src/App.jsx and sibling components"
          ],
          "drop_data": [
            "data/*.parquet — drop your own parquets here"
          ],
          "env": {
            "OPENAI_API_KEY": "Required for the classify and answer stages. Lives in .env next to run_app.sh.",
            "CODEX_CLI": "Optional override for the CLI binary path; defaults to `codex` on PATH."
          },
          "notes": [
            "Codex CLI must be installed on the machine running the backend. If you don't have it, use data_chatbot (pure API) instead.",
            "Same data-privacy posture as data_chatbot: only metadata.json reaches the model, never the rows."
          ]
        }
      }
    }
  ]
}
