# Frontend install / build artifacts
frontend/node_modules/
frontend/dist/

# Backend Python cache
backend/__pycache__/
backend/*.pyc

# Local environment — .env.example is the template that ships
.env

# Runtime outputs (per-receipt scan results + uploaded source images)
backend/agent_results/
backend/document_input/
