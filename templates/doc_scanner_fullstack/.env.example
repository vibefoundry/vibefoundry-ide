# Copy this file to `.env` (same folder) and fill in your key.
# .env is gitignored; .env.example is the template that ships with the project.
# This file lives at the PWA app root — sibling to backend/ and frontend/ —
# which is where backend/prompt.py's _load_dotenv() looks for it.

OPENAI_API_KEY=
