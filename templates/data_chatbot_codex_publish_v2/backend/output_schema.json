{
  "type": "object",
  "additionalProperties": false,
  "required": ["code", "response"],
  "properties": {
    "code": {
      "type": "string",
      "description": "Self-contained Python that answers the user's question against the dataset. The names `pl` (polars), `DATA_PATH` (the parquet to read), and `RESULT_PATH` (where to write the answer parquet) are already defined. The code must read from DATA_PATH with pl.scan_parquet, compute the answer lazily with streaming, and write the resulting DataFrame to RESULT_PATH with .write_parquet(RESULT_PATH). No imports other than polars, no printing, no other file or network access."
    },
    "response": {
      "type": "string",
      "description": "One or two plain sentences, in plain language, describing what the code computes and any scope it applies. Do not invent filters or values; describe only what the user's question and the conversation actually call for. No code, no markdown."
    }
  }
}
